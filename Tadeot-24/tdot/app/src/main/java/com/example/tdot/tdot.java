package com.example.tdot;

import android.util.Log;
import android.widget.Button;
import android.widget.GridLayout;

public class tdot {

    /*public static Button square1;
    public static void getButtonSize(GridLayout gridLayout) {
        // Erhalte die Layoutparameter des Buttons
        GridLayout.LayoutParams params = (GridLayout.LayoutParams) square1.getLayoutParams();

        // Berechne die Breite und Höhe des Buttons in Pixeln
        int buttonWidth = (int) (params.width * square1.getResources().getDisplayMetrics().density);
        int buttonHeight = (int) (params.height * square1.getResources().getDisplayMetrics().density);

        // Gib die Größe aus (ersetze dies durch deine eigene Verarbeitung)
        Log.d("Button Size", "Width: " + buttonWidth + "px, Height: " + buttonHeight + "px");
    }*/
}

